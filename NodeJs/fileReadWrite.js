// Add Record Into The File

    /* 
    
        let output = " Name : " + name + " Surname : " + surname + " Age : " + age;
        let data = fs.readFileSync('output.txt', "utf-8");
        console.log(data);
        if (data != null) {
            fs.appendFileSync('output.txt', output);
        }
        fs.writeFileSync('output.txt',output);
        const params = { code: "200", messgae: "Successful" };
        res.render("index", params);

    */